@model IEnumerable<WinS7Data.DataLibrary.Models.Recipe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DatConfig.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DatHE.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DatN2.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DatWerkzeug.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ToolCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ToolName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DatConfig.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatHE.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatN2.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatWerkzeug.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ToolCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ToolName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
